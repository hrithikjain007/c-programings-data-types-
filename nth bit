//to get the nth bit of a number

#include<stdio.h>
void main()
{
   unsigned int a,n,b,c,d,i;
	printf("\n enter the number\n");
		scanf("%d",&a);
	printf("\nenter the position\n");
		scanf("%d",&n);
	if(a&(1<<n))
			printf("%d bit is 1",n);

			else
			printf("%d bit is 0",n);
}
